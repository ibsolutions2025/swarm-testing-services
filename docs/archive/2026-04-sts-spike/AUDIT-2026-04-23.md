# End-to-end audit — 2026-04-23 06:28 UTC

Findings from Cash's 6-layer audit + follow-up Cowork schedule check. Use this to resume next session without re-running the audit.

## TL;DR

STS infrastructure (scanner / Supabase / API / UI) is healthy. The **AWP swarm driver is effectively dead** — the flywheel that feeds `lifecycle_results` is barely producing. Nothing catastrophic if left overnight; also nothing meaningful will accrue without a driver fix.

## Infra health ✅

| Component | State |
|---|---|
| sts-scanner on VPS | pm2 id 16, online, uptime ~72 min at audit time, 1 restart, 0 errors, 15-min loop |
| lifecycle_results rows | 658 for `project_id='awp'` |
| Scanner cadence | 1–2 upserts per 15-min cycle |
| On-chain ↔ DB alignment | 5/5 recent JobNFT tokenIds (654–658) match DB rows; `nonSwarm=0` |
| API `/api/test-results/lifecycle` | HTTP 200, live data |
| Dashboard route | HTTP 307 → /login (auth redirect, not 500) |
| Vercel deploy | commit 1c0f884 Ready |
| Agent dir workspace mirror | IDENTITY + SOUL + USER landed; openclaw.json NOT recoverable (VPS never had them at agent root) |

## Data completeness gap ⚠️

Sample of 5 most recent rows (654, 656, 657, 658, plus 647 older for variety):

- `step_audits`: **NULL in 5/5** — scanner doesn't produce them
- `cell_audit`: **NULL in 5/5** — scanner doesn't produce them
- `agent_wallets`: populated 5/5 (as `{poster, worker, validator}` object, not array)
- `status`: all `running` in the sample (not a bug — scanner terminal-state logic may not fire until events close)

**UI impact:** The Transactions drawer's "Cell audit" and "Step audits" collapsible sections will NEVER render until the scanner produces those JSONB blobs. Not a rendering bug, but the "audit" half of the demo is vestigial.

## Flywheel gap 🔴

| Expected driver | State |
|---|---|
| `awp-conductor` Cowork scheduled task | **DISABLED 2026-04-22** (note: Kimi single-call limit broke fleet dispatch; supposed to fall back to `awp-agent-loop.mjs` in Windows Task Scheduler) |
| `awp-matrix-audit` Cowork scheduled task | **DISABLED** (note: replaced by `awp-agent-loop.mjs`) |
| `OpenClaw-awp-test-*` Windows Scheduled Tasks | **NONE FOUND** — only 4 infra tasks exist |
| VPS `auto-cycle.mjs` per-agent last-run.json | **Stale Apr 15–18** — agents not self-cycling |
| `awp-agent-loop.mjs` | **UNKNOWN LOCATION** — disable notes reference it as "the real swarm" but Cash's audit didn't find it running |

New on-chain activity IS happening: jobs 657 (scenario s00-in-flight) and 658 (s00-in-flight) posted today by wallet `0x35bd1F28e93afdd929b82fF47612d00BEfc136CE` (Spark / awp-test-1). **Something** is posting — we don't know what. Rate: ~2 jobs in 2 hours = ~24/day if sustained.

## Outstanding fixes (ordered by blast radius)

### 1. Restart swarm driver (HIGH)

Find what's actually posting jobs 657/658, or hook up a replacement. Options in order of preference:

- **A.** `awp-agent-loop.mjs` already lives on VPS as pm2/systemd/cron — just needs to be discovered and confirmed running. Check: `pm2 list`, `crontab -l`, `systemctl list-units`, `/etc/cron.d/`.
- **B.** Per-agent OpenClaw crons — 7 single-tool-call Kimi tasks each firing every 10 min (memory `feedback_operator_teams_pattern` + `feedback_kimi_not_for_orchestration` confirm this pattern works; Kimi single-call limit doesn't apply when each cron IS the single call).
- **C.** Re-enable `awp-conductor` but route dispatch through a non-Kimi model (Opus/Sonnet) that can multi-turn. Expensive.

### 2. Scanner `step_audits` + `cell_audit` (MEDIUM)

Scanner at `/root/sts-scanner/` on VPS needs to produce audit JSONB when lifecycle events are captured. Spec unclear — does audit mean:

- Event-level integrity check (did the submission match the scenario expected state?)
- Per-step timing/gas/wallet-hygiene check?
- Aggregate pass/fail across all steps in a cell?

Need to define the shape first, then edit `scanner/sts-scanner.mjs` to emit it. Deferred — not blocking overnight.

### 3. Transactions drawer: separate `wallets` role display (LOW)

Schema has `wallets` JSONB (role-labeled: `{poster, validator, ...}`) distinct from `agent_wallets` (flat list). UI currently flattens everything via `extractAgentWallets()`. A small addition to `TransactionDrawer` could show role labels: "Poster: 0x… / Validator: 0x…" instead of an unlabeled list. Fully cosmetic.

### 4. `openclaw.json` backfill for agent dirs (LOW)

Cash couldn't recover them from VPS (they don't exist outside `awp-config/` which is key-holding and blocked). Options:

- Write 7 minimal `openclaw.json` files per persona in the STS repo from scratch — derive `model: 'moonshotai/Kimi-K2.6-TEE'` from fleet standard, list allowed tools based on each persona's archetype
- Skip — Tools tab just shows empty state forever

## Sources

- Cash audit task `task_1776925414532_34` — full report in gateway logs 2026-04-23T06:29:01Z
- Cash enrichment task `task_1776924202211_32` — 14 files changed, commit 48894e2
- Cowork scheduled-tasks list — queried 2026-04-23T06:~50Z
